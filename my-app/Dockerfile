# Sets the base image for the container
FROM nginx:latest

# Sets the working directory inside the container
WORKDIR /app

# Declares that the container will listen on port 80 (default HTTP port)
# It does not actually publish the port but acts as metadata for tools like Docker Compose or Kubernetes
EXPOSE 80